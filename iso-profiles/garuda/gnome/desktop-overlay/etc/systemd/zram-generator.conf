# zram swap (Bazzite-style): compressed RAM swap instead of disk swap.
# Half of RAM, capped at 16 GiB, zstd compression.
[zram0]
zram-size = min(ram / 2, 16384)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
