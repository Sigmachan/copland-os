# Early KMS for NVIDIA so the GPU is initialized in the initramfs (no mode-switch flicker,
# clean Wayland/Plymouth handoff). Garuda uses dracut (garuda-dracut-support).
force_drivers+=" nvidia nvidia_modeset nvidia_uvm nvidia_drm "
