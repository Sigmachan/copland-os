interface:
  display_name: "Copland Os"
  short_description: "Repo-specific patterns and workflows for copland-os"
  default_prompt: "Use the copland-os repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true