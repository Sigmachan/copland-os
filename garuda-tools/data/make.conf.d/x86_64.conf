carch="x86_64"
chost="x86_64-pc-linux-gnu"
# Copland local builds target the Ryzen 9 9950X3D. UPM selects LLVM first and
# retries GCC when required, so compiler-specific LTO flags belong in PKGBUILDs.
cflags="-march=znver5 -O3 -pipe -fstack-protector-strong -fno-plt"
